# DesktopNote 使用指南

## 基本操作

- 在便签中直接输入、选择、复制或粘贴富文本。
- 拖动顶部彩色条移动窗口；拖动边缘或右下角调整大小。
- 鼠标移入或激活便签时显示工具栏，点击“固定”可让工具栏保持显示。
- 右键便签打开窗口菜单；右键托盘图标管理全部便签。

## 全局热键

- `Ctrl+Alt+N`：无论程序是否在前台，一键新建并聚焦一张便签（快速捕捉）。
- `Ctrl+Alt+H`：显示或隐藏全部便签。

两者在托盘菜单都有对应入口；若组合键与其他软件冲突导致注册失败，DesktopNote 仍会正常运行，只是该热键不生效。

## 工具栏

- 字体和字号作用于当前选区；没有选区时作为后续输入格式。
- “字色”“背景”“边框”使用预设色板直接选择，当前颜色带选中框。
- 滑块分别调整背景不透明度、内容边距与段落间距。
- 拖动工具栏左侧手柄可移动便签。
- “菜单”提供窗口模式、鼠标穿透、位置锁定、新建和删除。

## 窗口模式

- 普通模式：与一般桌面窗口相同。
- 置顶模式：保持在其他普通窗口上方。
- 嵌入桌面：尝试挂接 Explorer 的 WorkerW；Explorer 状态不支持时自动退回可见窗口，不会丢失便签。

启用鼠标穿透后便签不可直接点击。可从托盘菜单选择“关闭所有鼠标穿透”恢复。位置锁定也可从托盘菜单统一解除。

## 保存与恢复

内容或设置改变后约 500 ms 自动保存；失焦、退出和系统关机前会立即刷新。主文件是 `%APPDATA%\DesktopNote\data.json`，旁边的 `data.json.bak` 和 `backups` 目录用于损坏恢复与迁移备份。

如需搬迁数据，请先从托盘退出 DesktopNote，再复制整个 `%APPDATA%\DesktopNote` 目录。
